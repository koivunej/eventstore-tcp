(function() {var implementors = {};
implementors["eventstore_tcp"] = ["impl Service for <a class="struct" href="eventstore_tcp/struct.EventStoreClient.html" title="struct eventstore_tcp::EventStoreClient">EventStoreClient</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
